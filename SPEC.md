# 2分にしてね — Product Specification

## 1. 概要

**アプリ名:** 2分にしてね  
**英語名:** Mitene Video Converter  
**正式表記:** 2分にしてね - Mitene Video Converter  
**サブタイトル:** みてねへのアップロード用に動画を分割・軽量化

> 動画を放り込むと、みてねに上げられる形になる。

家族アルバム「みてね」へアップロードする動画を、ユーザーが形式・解像度・コーデック・ビットレート・分割時間を意識せずに整えるmacOSアプリ。処理はすべてMac上で完結し、元動画は変更しない。

## 2. V1の対象と非目標

対象は、動画を編集したい人ではなく「この動画をみてねに上げたいだけの人」。macOS 14 Sonoma以降を対象とし、Apple Siliconを優先する。Intel MacのUniversal Binary化は追加コストが小さい場合に行う。

V1では、動画編集・トリミング・シーン検出・AIハイライト・字幕・BGM・色調整・手動エンコード設定・任意の分割時間・他サービス向けプリセット・みてねへの直接ログイン／投稿・自動アップデート・Windows版を扱わない。

## 3. 入力

- `.mp4` / `.mov` / `.m4v`
- AVFoundationで正常にデコードできる動画
- H.264 / HEVC、SDR / HDR / Dolby Vision、横動画 / 縦動画、無音動画
- 複数ファイルの同時ドラッグ＆ドロップ

## 4. 出力プロファイル

入力に関係なく、V1では次へ正規化する。

| 項目 | ルール |
| --- | --- |
| コンテナ | MP4 |
| 映像 | H.264 / AVC、8bit、4:2:0 |
| 色 | SDR / BT.709 |
| 解像度 | 横は最大1280×720、縦は最大720×1280、アスペクト比維持、アップスケールなし |
| フレームレート | 最大30fps。30fps以下は原則維持 |
| 音声 | AAC-LC、最大2ch、目標128kbps。無音も処理可能 |
| 長さ | 1本119秒以下 |

HDR入力はSDRへ変換する。V1では「変換不要な動画の無劣化コピー」は行わず、結果を一定にすることを優先する。

## 5. 分割・日時・ファイル名

必要本数を `ceil(duration / 119)` で決め、動画全体を均等分割する。120秒は約60秒×2、300秒は約100秒×3とし、極端に短い最後の動画を作らない。

撮影日時は、動画コンテナ内のcreation metadata、ファイル作成日時、ファイル更新日時の順で取得する。分割後はセグメント開始位置を加算し、MP4コンテナとQuickTime互換metadataへ反映する。

出力先は `~/Movies/2分にしてね/`。元のファイル名に `_mitene` を付け、分割時は `_mitene_01` のように連番を付ける。既存ファイルは上書きせず、重複時は自動回避する。

## 6. UIと処理

起動直後は「2分にしてね」「動画をここにドロップ」「または動画を選ぶ」だけを提示する。変換中はキューを1本ずつ順番に処理し、ユーザーにはファイル名・進捗・キャンセルだけを表示する。1本の失敗でバッチ全体を停止しない。

完了後は「写真に追加」「Finderで表示」「動画を追加」を表示する。PhotoKitで写真ライブラリへの追加を行い、みてねへの直接投稿は行わない。

## 7. 実装構造

- `VideoAnalyzer`: duration、表示方向、解像度、codec、fps、音声、撮影日時を取得
- `ConversionPlanner`: 出力サイズ、fps、セグメント範囲、日時を決める純粋ロジック
- `VideoConverter`: AVFoundationでH.264 / MP4へ変換
- `PhotoLibraryExporter`: PhotoKitで写真へ追加
- `AppModel` / SwiftUI: 入力キューとユーザー操作を所有

FFmpegやクラウド処理、アカウント、テレメトリ、広告SDKは導入しない。

## 8. V1完了条件

ドラッグ＆ドロップ、複数入力、4K→720p、HEVC→H.264、HDR→SDR、60fps→最大30fps、119秒以下への均等分割、縦横維持、撮影日時と分割日時の維持、原本保護、Finder表示、写真追加を満たす。みてね無料版への実アップロード成功は実機受入試験で確認する。
