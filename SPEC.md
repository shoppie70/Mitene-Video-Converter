# 2分にしてね 仕様書

## プロダクトの定義

| 項目 | 内容 |
| --- | --- |
| アプリ名 | 2分にしてね |
| 英語名 | Mitene Video Converter |
| 正式表記 | 2分にしてね - Mitene Video Converter |
| サブタイトル | みてねへのアップロード用に動画を分割・軽量化 |

> 動画を放り込むと、みてねに上げられる形になる。

「2分にしてね」は、家族アルバム「みてね」に動画をアップロードしたい人のためのmacOSアプリ。撮影機材や元の形式に関係なく、動画をみてね用の扱いやすいファイルへ整える。

ユーザーに動画形式、解像度、フレームレート、コーデック、ビットレート、分割時間を選ばせない。判断する場所をアプリに集め、「動画を入れる」ことだけをユーザーの操作にする。

## 対象ユーザーと課題

動画編集をしたい人ではなく、撮影した動画を家族アルバムへアップロードしたい人を対象とする。iPhone、Android、GoPro、DJI、デジタルカメラ、ビデオカメラなど、撮影機材は限定しない。

長い動画、大きな動画、HEVC動画、高フレームレート動画は、そのままではアップロード前に確認や変換が必要になる。アプリはその判断と作業を引き受ける。

## ユーザーフロー

1. ユーザーが動画をドロップする、またはファイル選択で追加する。
2. アプリが動画を解析し、変換計画を作る。
3. 動画を1本ずつ変換し、必要なら均等に分割する。
4. 変換結果を `~/Movies/2分にしてね/` に保存する。
5. ユーザーがFinderで確認するか、写真アプリへ追加する。
6. ユーザーがiPhoneのみてねアプリからアップロードする。

元動画は読み取り専用として扱い、変換中も変換後も変更・削除しない。

## 入力

### ファイル形式

- `.mp4`
- `.mov`
- `.m4v`

AVFoundationで正常にデコードできる動画を対象とする。複数ファイルを一度に追加でき、1本の失敗で他の動画の処理を止めない。

### 想定する動画

H.264、HEVC、SDR、HDR、Dolby Vision、横動画、縦動画、24fps、30fps、60fps、4K、1080p、720p、音声のない動画を扱う。縦横はストリームの幅・高さだけでなく、QuickTime Transformなどの回転情報を含めて判断する。

## 出力契約

すべての入力を同じ「みてね用プロファイル」へ正規化する。入力がすでに条件を満たしていても、無劣化コピーに切り替えず、出力のばらつきを抑える。

| 項目 | ルール |
| --- | --- |
| コンテナ | MP4 |
| 映像コーデック | H.264 / AVC |
| ピクセル形式 | 8bit / 4:2:0 |
| 色域 | SDR / BT.709 |
| 解像度 | 横動画は最大1280×720、縦動画は最大720×1280 |
| アスペクト比 | 維持する。元動画より大きくしない |
| フレームレート | 30fpsを上限とする。30fps以下は原則として維持する |
| 音声 | AAC-LC、最大2ch、目標128kbps |
| 音声なし | 無音動画として処理する |
| 1本の長さ | 119秒以下 |

HDR、HLG、PQ、Dolby Visionの入力はSDRへ変換する。出力で白浮き、暗部の潰れ、過度な彩度変化、ハイライトの不自然な白飛びを起こさないことを重視する。

### 代表的な変換

| 入力 | 出力の考え方 |
| --- | --- |
| 3840×2160 / 59.94fps / HEVC | 1280×720 / 30fps以下 / H.264 / SDR |
| 2160×3840相当の縦動画 | 720×1280以下 / H.264 / SDR |
| 1920×1080 | 1280×720 |
| 640×480 | 640×480のまま |

## 分割

1本の最大長は119秒とする。動画全体の長さを `duration` としたとき、必要本数を次で決める。

```text
segmentCount = ceil(duration / 119)
segmentDuration = duration / segmentCount
```

この計算により、120秒は約60秒×2、300秒は約100秒×3になる。単純に119秒ずつ切って、最後に極端に短いファイルを作る方式は採用しない。フレーム境界による微調整は許容するが、すべての出力が120秒未満であることを優先する。

## 撮影日時とファイル名

### 撮影日時

次の優先順位で元動画の日時を取得する。

1. 動画コンテナ内のcreation metadata
2. ファイル作成日時
3. ファイル更新日時

分割した動画には、元動画の日時へセグメント開始位置を加算する。MP4コンテナとQuickTime互換のcreation metadataへ反映し、可能な場合はファイルシステムの作成日時・更新日時も合わせる。

### ファイル名

元ファイル名を維持し、拡張子の前へ `_mitene` を付ける。

```text
DJI_20260903_120002.mp4
→ DJI_20260903_120002_mitene.mp4
```

分割時は2桁の連番を付ける。

```text
DJI_20260903_120002_mitene_01.mp4
DJI_20260903_120002_mitene_02.mp4
```

既存ファイルは上書きせず、同名がある場合は重複しない名前を自動生成する。

## 画面

### 初期画面

表示する情報を絞り、次の操作を主役にする。

- 2分にしてね
- 動画をここにドロップ
- または動画を選ぶ
- みてねにアップロードしやすい動画に、いい感じに変換します

コーデック、ビットレート、CRF、HDRなどの技術用語を初期画面へ出さない。

### 変換中

複数動画はキューとして表示し、1本ずつ順番に処理する。表示するのはファイル名、処理状態、進捗、キャンセル操作とする。技術情報の詳細表示は必須としない。

### 完了後

出力本数を表示し、次の操作を提供する。

- 写真に追加
- Finderで表示
- 動画を追加

### エラー

技術的なエラーコードをそのまま見せない。入力を読めない場合、壊れている可能性がある場合、保存先の容量が足りない場合、写真アプリへの追加に失敗した場合を、ユーザーが次に取る行動が分かる日本語で表示する。バッチ内の他の動画は可能な限り処理を続ける。

## 保存と写真アプリ

デフォルト保存先は `~/Movies/2分にしてね/`。変換単位でサブフォルダは作らず、同じフォルダへ保存する。

完了後の「写真に追加」はPhotoKitを利用する。写真アプリへ追加した後の、iPhoneとの同期やみてねへのアップロードはユーザーが行う。アプリからみてねへ直接ログイン・直接投稿はしない。

## プライバシーと境界

動画の変換はすべてMac上で行い、動画を外部サーバーへ送信しない。アカウント、クラウド同期、動画収集、テレメトリ、広告SDKは持たない。

「2分にしてね - Mitene Video Converter」は、家族アルバム「みてね」の公式アプリではない。みてねの利用を補助する独立したツールとして、直接投稿を行わないことを明示する。

## 技術方針

- UI: SwiftUI
- 動画解析・変換: AVFoundation / VideoToolbox
- 写真アプリ連携: PhotoKit
- 外部動画変換バイナリ: 使用しない

責務は次の4つに分ける。これ以上のレイヤーやRepositoryパターンは、現在の目的には追加しない。

- `VideoAnalyzer`: 動画の長さ、表示方向、解像度、fps、codec、音声、撮影日時を取得する
- `ConversionPlanner`: 出力サイズ、fps、分割範囲、日時を決める
- `VideoConverter`: AVFoundationで実際の変換を行う
- `PhotoLibraryExporter`: 生成した動画を写真アプリへ追加する

## 対象外

動画編集、トリミング、シーン検出、AIによる要約・ハイライト、字幕、BGM、色調整、手動エンコード設定、任意の分割時間、YouTubeやInstagram向けプリセット、みてねへの直接投稿、自動アップデート、Windows版は扱わない。
