# Product Demo Utility LP Design System

> 静かな白い空間に、ひとつの製品体験と、ひとつの青いアクション。

## この文書の役割

この文書は、「2分にしてね - Mitene Video Converter」のLPから抽出した、macOSユーティリティ向けのデザインシステムです。

特定の製品名、コピー、数値、URLを別製品へ流用するためのテンプレートではありません。対象製品の仕様とユーザー体験を優先し、その価値を同じ品質で見せるための判断基準として使用します。

スクリーンショットは完成形の見た目を伝え、この文書は、その見た目を成立させている意図と制約を伝えます。両方がある場合は、次の順序で判断します。

1. 対象製品の事実とユーザー体験
2. 承認された完成版スクリーンショット
3. この `DESIGN.md` に記載した原則
4. 実装者の判断

## デザインの核

### 5秒で用途が分かる

ファーストビューだけで、次の3点が理解できる状態をつくります。

- 何を入れるアプリなのか
- 何に変わるのか
- 何を押せば使い始められるのか

機能一覧から始めません。最初に見せるのは製品の結果です。

### 製品そのものを主役にする

ヒーローには、実アプリの画面、または実際の操作を正確に表した製品ビジュアルを置きます。抽象的なイラストや装飾だけでヒーローを成立させません。

製品ビジュアルには「入力」「アプリ」「完了」の流れを持たせます。ユーザーが説明文を読まなくても、何が起きるのか推測できることが重要です。

### 信頼は説明量ではなく、構造で伝える

明るい製品デモの後に、暗い安心設計のセクションを置きます。

この明暗の切り替えをLPで最も強い視覚的な変化とし、ローカル処理、元データの保護、プライバシーなど、その製品で最も重要な信頼要素を伝えます。

## ページ構成

ページは、次の順序を基本とします。

1. フローティングナビゲーション
2. 2カラムのヒーローと製品デモ
3. 3〜4項目のクイックファクト
4. 入力と出力を比較する変換セクション
5. ダークトーンの安心設計セクション
6. ダウンロード前の疑問に答えるFAQ
7. 製品名、短い結論、CTAだけのクロージング

各セクションは別々の機能を列挙するためではなく、ユーザーの理解を一段ずつ進めるために置きます。

デスクトップの全体高は、おおむね3,200〜4,500pxを目安とします。内容が少ないのに余白だけで長くしません。インストール手順など不可欠な説明がある場合は、価値説明の後に追加します。

## レイアウト

### 基本グリッド

- コンテンツ最大幅: `1180px`
- デスクトップ左右余白: `24px`以上
- モバイル左右余白: `16px`
- ヒーロー: デスクトップは原則2カラム
- 主要ブレークポイント: `980px`、`720px`

ヒーローの左側には結論とCTA、右側には製品デモを置きます。中央揃えの巨大見出しと製品画像を縦に積むだけの構成にはしません。

`980px`以下ではヒーローを1カラム化し、コピーの直後に製品デモを置きます。`720px`以下では変換前後、機能列、FAQをすべて自然な1カラムへ落とします。

## カラー

| 役割 | 色 | 用途 |
| --- | --- | --- |
| Ink | `#111216` | 見出し、主要テキスト、ナビゲーションCTA |
| Ink Soft | `#3f424a` | リード文、補助的な説明 |
| Muted | `#6f7480` | 注釈、仕様ラベル、二次情報 |
| Canvas | `#f7f8fb` | ページの基本背景 |
| Surface | `#ffffff` | 変換セクション、製品ウインドウ |
| Canvas Blue | `#eef4ff` | 出力、選択、変換後の状態 |
| Action Blue | `#0866ff` | 主要CTA、結果、操作の方向 |
| Night | `#111a2f` | 安心設計セクション |
| Night Text | `#abb6ca` | 暗い背景上の本文 |
| Rule | `#dfe2e8` | ヘアライン、情報の区切り |

青はアクション、変換後、信頼の補助に限定します。見出し全体や複数の装飾へ無差別に使いません。

ダークセクションは黒への機械的な反転ではなく、わずかに青みを持つ `Night` を使います。LP全体のプライバシーとネイティブアプリらしさを支える色です。

## タイポグラフィ

外部フォントは読み込みません。macOSでSF Proが使われ、その他の環境ではシステムフォントへ自然にフォールバックする構成にします。

```css
--display: "SF Pro Display", -apple-system, BlinkMacSystemFont, "Helvetica Neue", "Hiragino Sans", sans-serif;
--body: "SF Pro Text", -apple-system, BlinkMacSystemFont, "Helvetica Neue", "Hiragino Sans", sans-serif;
```

### 文字サイズの目安

| 役割 | サイズ |
| --- | --- |
| ヒーロー見出し | `clamp(48px, 5.5vw, 68px)` |
| セクション見出し | `clamp(36px, 5vw, 58px)` |
| リード文 | `18〜21px` |
| 本文 | `14〜17px` |
| ラベル、仕様 | `10〜12px` |

見出しはウェイトだけでなく、字間を詰めて密度をつくります。本文は十分なコントラストを保ち、薄いグレーを使いすぎません。

日本語の改行は意味の切れ目で固定します。「みてねサイズ／に。」のように助詞や句点だけを次の行へ送らないでください。デスクトップとモバイルの両方で実際に表示して確認します。

小さな英語ラベルは1セクションに1つまでを目安とします。すべてのセクションを英語ラベルから始めると、テンプレート感が生まれます。

## コンポーネント

### フローティングナビゲーション

- 画面上端から少し離して配置する
- 半透明の白、細い境界線、弱いぼかしを使う
- 左に製品アイコンと名前、中央または右に主要リンク、右端にCTAを置く
- CTA以外を強くしすぎない
- モバイルでは主要リンクを隠し、製品名とCTAだけを残す

### 製品アイコン

製品アイコンが実在する場合は、その画像を優先して使います。CSSで再現する場合も、ロゴではなく「アプリアイコン」として、角丸、内部構造、光の当たり方まで設計します。

アイコン単体をページ中で何度も繰り返しません。ナビゲーション、製品デモ、クロージングなど、役割が明確な場所に限定します。

### 製品デモ

製品デモは単なる白いウインドウではありません。次の状態を組み合わせ、操作の流れを一枚で示します。

- 入力ファイルを表す小さなフローティング要素
- 実アプリに近いウインドウ
- ドロップ領域または中心操作
- 完了状態を表す小さな結果要素

影は製品デモに集中させます。ページ中のすべてのカードに同じ影を付けません。

### クイックファクト

3〜4項目を横一列に並べ、製品の制約や安心材料を短く提示します。文章ではなく、値として読める内容を選びます。

例:

- 対応形式
- 出力形式
- 最大サイズや時間
- ローカル処理

実測していない削減率、処理速度、利用者数などは掲載しません。

### 変換前後

製品価値を最も具体的に伝えるセクションです。

- 左に入力ファイル
- 中央に変換を示すひとつのアクション
- 右に出力ファイル
- 下に実際の操作手順

入力と出力には、その製品で本当に保証できる属性だけを表示します。架空のファイルサイズや処理時間は使いません。

### 安心設計セクション

ページで唯一の大きなダークセクションです。プライバシーやデータ保護の主張を、ひとつの見出しとひとつの象徴的なビジュアルで伝えます。

その下に、関連する3つの利点を境界線で分けて並べます。独立した角丸カードにはしません。

### FAQ

左に見出し、右にアコーディオンを置きます。質問は、購入やダウンロードの直前に生じる現実的な不安だけに絞ります。

開閉記号はCSSまたは統一したSVGで描画し、絵文字を使いません。質問文全体がクリックできるようにします。

### クロージング

最後は製品アイコン、製品名、一文の結論、CTA、動作環境だけを中央に置きます。ここでは新しい説明を始めません。

## 角丸、境界線、影

| 対象 | 値の目安 |
| --- | --- |
| 小さな要素 | `10〜14px` |
| 製品ウインドウ | `20px` |
| 大きな演出要素 | `28px` |
| ボタン | `999px` |
| 境界線 | `1px solid #dfe2e8` |

角丸は階層を示すために使います。すべての要素を同じ角丸カードにしません。

強い影は製品ウインドウと主要CTAに限定します。情報カードは背景色と境界線で分離します。

## アイコン

- 絵文字は使わない
- 24pxの同一 `viewBox` を基本とする
- `fill: none`、丸い線端、同じ線幅で統一する
- 装飾ではなく、入力、出力、ダウンロード、保護などの意味を補う
- 実在ブランドのロゴを推測して描かない

## モーション

モーションは、製品デモ内の入力ファイルなど、操作の方向を伝えるひとつの要素へ集中させます。

- 常時動く要素は原則1つ
- 移動量は小さくする
- ボタンのホバーでレイアウトを動かさない
- スクロール演出を前提にしない
- `prefers-reduced-motion` で停止する

## コピー

### 見出し

製品名ではなく、ユーザーが得る結果を主語にします。

良い例:

> 長い動画を、みてねサイズに。

避ける例:

> 革新的な動画変換エクスペリエンス

### 本文

一つの段落では、一つの判断だけを伝えます。機能名を並べるより、「何を入れると、どうなるか」を書きます。

CTAは「無料ダウンロード」だけでなく、対象プラットフォームが分かる表記にします。

### 事実性

次の内容は、確認できない限り掲載しません。

- レビュー、星評価、利用者数
- 変換速度
- 圧縮後のファイルサイズ
- 成功率
- 公式サービスとの関係

## 他製品へ適用する方法

このデザインを別製品へ適用するときは、HTMLの文言だけを置換しません。最初に次の問いへ答えます。

1. ユーザーが入れるものは何か
2. 何に変わるのか
3. 最も面倒な既存作業は何か
4. 最も重要な安心材料は何か
5. ダウンロード前に不安になることは何か

回答をもとに、ヒーロー、変換前後、ダークセクション、FAQを作り直します。グリッド、余白、タイポグラフィの品質は引き継ぎますが、製品ビジュアルとコピーは対象製品専用にします。

### ImageDropへの適用例

- ヒーローの結論: 「iPhoneの写真を、使いやすいJPEGに。」
- 入力: `IMG_1234.HEIC`、4032×3024、HEIC
- 出力: `IMG_1234_compressed.jpg`、1980×1485、JPEG品質0.85
- 安心設計: 「写真は、Macから出さない。」
- 補助価値: メタデータ削除、元画像を上書きしない、複数画像対応
- 製品デモ: 画像をDockアイコンまたはアプリ画面へドロップすると、同じフォルダにJPEGができる流れ
- 初回起動案内: 製品価値の説明後に、既存のGatekeeper手順をコンパクトに配置する

これはコピー案であり、対象リポジトリの仕様書と実装を確認してから使用します。

## 禁止事項

- 中央揃えの巨大見出しと巨大画像を、縦に積むだけのヒーロー
- 内容に対して過剰なセクション高
- 小さな英語ラベルと巨大見出しの反復
- すべてを角丸カードに収めるBento Grid
- 装飾目的だけのグラデーション
- 複数のアクセントカラー
- 絵文字によるアイコン表現
- 架空の数値、レビュー、社会的証明
- 実アプリと異なる操作画面
- 意味の途中で切れる日本語見出し
- 既存LPのコピーとレイアウトを、別製品へそのまま複製すること

## 検証

完成時は、ソースコードだけで判断せず、実ブラウザで確認します。

- `1440px`: 全体の構図、余白、ページ高、ヒーローの2カラム
- `1024px`: 2カラムから1カラムへ移る境界
- `768px`: ナビゲーションと変換前後の収まり
- `375px`: 日本語改行、CTA、横スクロール、タップ領域

さらに次を確認します。

- 横スクロールがない
- 見出しが意味の途中で改行されていない
- CTAと外部リンクが正しい
- FAQが開閉する
- コンソールエラーがない
- キーボードフォーカスが見える
- `prefers-reduced-motion` に対応している
- 画像には適切な代替テキストがある
- `git diff --check` が通る
- 公開リポジトリに個人情報、秘密情報、絶対パス、生成物を含めていない

## この実装での参照先

- 構造: `docs/index.html`
- トークンとレスポンシブ設計: `docs/styles.css`
- 完成形: デスクトップ版とモバイル版のフルページスクリーンショット

この文書と実装に差がある場合、目視確認で承認された完成形を優先し、その差が再利用すべき判断であれば、この文書へ反映します。
